[tool.poetry]
name = "api_gateway"
version = "0.1.0"
description = "Aura API Gateway Service"
authors = ["Mohammad Atashi"]

[tool.poetry.dependencies]
python = ">=3.11, <3.13"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"


